from sqlalchemy import Column, Integer, String, ForeignKey\nfrom sqlalchemy.orm import relationship\nfrom sqlalchemy.ext.declarative import declarative_base\n\nBase = declarative_base()\n\nclass User(Base):\n    __tablename__ = 'users'\n\n    id = Column(Integer, primary_key=True)\n    username = Column(String, unique=True, nullable=False)\n    email = Column(String, unique=True, nullable=False)\n    reputation = relationship('Reputation', back_populates='user')\n    skills = relationship('SkillListing', back_populates='user')\n    freelance_jobs = relationship('FreelanceJob', back_populates='user')\n    disputes = relationship('Dispute', back_populates='user')\n\nclass Reputation(Base):\n    __tablename__ = 'reputations'\n\n    id = Column(Integer, primary_key=True)\n    user_id = Column(Integer, ForeignKey('users.id'), nullable=False)\n    score = Column(Integer, nullable=False)\n    user = relationship('User', back_populates='reputation')\n\nclass SkillListing(Base):\n    __tablename__ = 'skill_listings'\n\n    id = Column(Integer, primary_key=True)\n    user_id = Column(Integer, ForeignKey('users.id'), nullable=False)\n    skill_name = Column(String, nullable=False)\n    user = relationship('User', back_populates='skills')\n\nclass Session(Base):\n    __tablename__ = 'sessions'\n\n    id = Column(Integer, primary_key=True)\n    user_id = Column(Integer, ForeignKey('users.id'), nullable=False)\n    session_data = Column(String, nullable=False)\n    user = relationship('User')\n\nclass FreelanceJob(Base):\n    __tablename__ = 'freelance_jobs'\n\n    id = Column(Integer, primary_key=True)\n    user_id = Column(Integer, ForeignKey('users.id'), nullable=False)\n    job_title = Column(String, nullable=False)\n    description = Column(String, nullable=False)\n    user = relationship('User', back_populates='freelance_jobs')\n\nclass Dispute(Base):\n    __tablename__ = 'disputes'\n\n    id = Column(Integer, primary_key=True)\n    user_id = Column(Integer, ForeignKey('users.id'), nullable=False)\n    issue_description = Column(String, nullable=False)\n    user = relationship('User', back_populates='disputes')\n